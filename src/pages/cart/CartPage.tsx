import { PageShell } from '@/shared/ui/PageShell'

export function CartPage() {
  return (
    <PageShell
      title="Cart"
      description="Shopping cart state will live in a dedicated feature module."
    />
  )
}
