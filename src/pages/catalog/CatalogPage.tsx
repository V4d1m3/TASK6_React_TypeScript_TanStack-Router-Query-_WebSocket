import { PageShell } from '@/shared/ui/PageShell'

export function CatalogPage() {
  return (
    <PageShell
      title="Catalog"
      description="Product list with search, filters, sorting, and pagination."
    />
  )
}
